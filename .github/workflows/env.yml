name: macos-latest default environment
on: 
  workflow_dispatch:

jobs:
  show-env:
    runs-on: macos-latest
    steps:
      - name: touch brew
        run: brew --version
      - run: java -version
      - run: node --version
      - run: python --version
